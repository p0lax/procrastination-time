import Icon from 'components/Icon/Icon';
import styles from './Spin.module.css';

const Spin = () => (
  <div className={styles.spin}>
    <Icon type="spin" size="md" />
  </div>
);

export default Spin;
